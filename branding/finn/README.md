# Branding — Scoutkåren Finn (placeholder)

Kår identity and branding are configurable and default to Finn (CLAUDE.md §13).
Vendor Finn's logo and colours here from scoutkarenfinn.se — **committed assets,
never fetched at build or run time**. Keep this directory swappable so another
kår drops in its own `branding/<kar>/`.

Expected files (add during branding work):
- `logo.svg` / `logo.png`
- `colors.json` (primary/accent, light + dark)

This is a placeholder so the path exists; no real assets are vendored yet.
