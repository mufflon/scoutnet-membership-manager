# TEMPLATE ONLY — never commit real keys (Hard rule #1). Create the real Secret
# out of band, e.g.:
#   kubectl create secret generic scoutnet-membership-manager-secrets \
#     --from-literal=SCOUTNET_MEMBERLIST_KEY=... \
#     --from-literal=SCOUTNET_ORGANISATION_GROUP_KEY=... \
#     --from-literal=SCOUTNET_DATABASE_URL=postgresql+psycopg://user:pass@host/db
apiVersion: v1
kind: Secret
metadata:
  name: scoutnet-membership-manager-secrets
type: Opaque
stringData:
  SCOUTNET_MEMBERLIST_KEY: "REPLACE_ME"
  SCOUTNET_ORGANISATION_GROUP_KEY: "REPLACE_ME"
  SCOUTNET_DATABASE_URL: "postgresql+psycopg://REPLACE_ME"
